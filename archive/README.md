# Archive

Superseded code, kept for reference only. Not maintained.

- **`motion_tracker_legacy.py`** — the original `Video_util` implementation.
  Replaced by the `VideoUtil` version now at `../motion_tracker.py`, which has the
  same features (including camera-motion stabilization) plus cleaner structure,
  proper error handling, and no deprecated pandas calls. Do not build on this file.
